# Local data policy

`data/local/` contains datasets and generated local data that must not be
automatically added to Git. Only manifests and this README are intentionally
trackable. The physical location and provenance of migrated local files are
recorded in `docs/migration/LOCAL_MIGRATION.csv`.
