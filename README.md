# LTD-Attack

Research repository for the LTD-Attack website-fingerprinting study.

The active code is organized under `src/`, operational entry points under
`scripts/`, experiment metadata under `experiments/`, and historical code under
`legacy/`. Local datasets are kept outside the tracked source tree under
`data/local/`; see `docs/migration/` for the physical migration record and
pending path work.
